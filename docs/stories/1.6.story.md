# Story 1.6: Security Hardening (Rate Limit + Password Validation)

**Epic:** [EPIC-1](../epics/epic-1-auth.md)  
**Pontos:** 8  
**Status:** To Do  
**Prioridade:** Crítico  
**Criado:** 2026-07-07

---

## Descrição

Implementar melhorias críticas de segurança no sistema de autenticação JWT custom, removendo riscos bloqueantes para produção: fallback inseguro do `JWT_SECRET`, ausência de rate limiting, validação fraca de senha e ausência de proteção adicional para endpoints sensíveis.

Esta story nasce da revisão técnica registrada em [`docs/analise/revisao-sistema-autenticacao-2026-07-07.md`](../analise/revisao-sistema-autenticacao-2026-07-07.md).

---

## Contexto

A decisão arquitetural confirmada é **remover/evitar Supabase Auth** e manter autenticação custom com:

- Fastify
- PostgreSQL direto
- bcrypt
- JWT
- cookies no frontend Nuxt

Com isso, a responsabilidade por hardening de autenticação passa a ser do próprio projeto. Esta story cobre as correções mínimas para reduzir riscos de força bruta, senhas fracas e configuração insegura.

---

## Tarefas (Checklist)

### Backend (`apps/api`)

- [ ] Remover fallback inseguro `process.env.JWT_SECRET || 'secret'`
  - [ ] Corrigir `apps/api/src/routes/auth.ts`
  - [ ] Corrigir `apps/api/src/middleware/auth.ts`
  - [ ] Centralizar leitura do segredo em `apps/api/src/config/jwt.ts`
  - [ ] Falhar na inicialização se `JWT_SECRET` não estiver definido

- [ ] Implementar rate limiting
  - [ ] Instalar/configurar `@fastify/rate-limit`
  - [ ] Definir limite global razoável para API
  - [ ] Definir limite específico para `POST /auth/login`
  - [ ] Definir limite específico para `POST /auth/register`
  - [ ] Retornar erro `429` com mensagem controlada

- [ ] Implementar validação robusta de senha
  - [ ] Criar helper `validatePassword`
  - [ ] Exigir mínimo de 8 caracteres
  - [ ] Exigir letra maiúscula
  - [ ] Exigir letra minúscula
  - [ ] Exigir número
  - [ ] Exigir caractere especial
  - [ ] Aplicar em `POST /auth/register`

- [ ] Melhorar logs de auditoria
  - [ ] Logar tentativas de login bem-sucedidas
  - [ ] Logar tentativas de login falhas sem expor senha
  - [ ] Incluir `agentId`, `tenantId`, IP e user-agent quando possível

### Frontend (`apps/web`)

- [ ] Tratar erro `429` em `useAuth()`
- [ ] Exibir mensagem amigável em caso de muitas tentativas
- [ ] Validar força da senha no formulário de cadastro antes do submit
- [ ] Substituir `window.location.href = '/'` por `navigateTo('/', { replace: true })`

### Documentação

- [ ] Atualizar `.env.example` com `JWT_SECRET`
- [ ] Registrar decisão de segurança no relatório de autenticação
- [ ] Atualizar checklist desta story ao concluir implementação

---

## Critérios de Aceite

- [ ] A API não inicia se `JWT_SECRET` estiver ausente
- [ ] Não existe mais fallback para `'secret'` em código de autenticação
- [ ] `POST /auth/login` aplica rate limiting e retorna `429` ao exceder limite
- [ ] `POST /auth/register` aplica rate limiting e retorna `429` ao exceder limite
- [ ] Registro rejeita senhas fracas com mensagem clara
- [ ] Login continua funcionando com credenciais válidas
- [ ] Registro continua criando tenant + agent admin dentro de transação
- [ ] Frontend apresenta mensagem amigável para rate limit
- [ ] Fluxos principais de autenticação passam nos testes existentes

---

## Notas Técnicas

### Validação mínima de senha

```ts
type PasswordValidationResult = {
  valid: boolean
  error?: string
}

export function validatePassword(password: string): PasswordValidationResult {
  if (password.length < 8) {
    return { valid: false, error: 'Senha deve ter pelo menos 8 caracteres' }
  }

  if (!/[A-Z]/.test(password)) {
    return { valid: false, error: 'Senha deve conter pelo menos uma letra maiúscula' }
  }

  if (!/[a-z]/.test(password)) {
    return { valid: false, error: 'Senha deve conter pelo menos uma letra minúscula' }
  }

  if (!/[0-9]/.test(password)) {
    return { valid: false, error: 'Senha deve conter pelo menos um número' }
  }

  if (!/[^A-Za-z0-9]/.test(password)) {
    return { valid: false, error: 'Senha deve conter pelo menos um caractere especial' }
  }

  return { valid: true }
}
```

### JWT secret obrigatório

```ts
const JWT_SECRET = process.env.JWT_SECRET

if (!JWT_SECRET) {
  throw new Error('JWT_SECRET environment variable is required')
}
```

---

## Riscos

| Risco                                                | Impacto | Mitigação                                        |
| ---------------------------------------------------- | ------- | ------------------------------------------------ |
| Rate limit muito agressivo afetar usuários legítimos | Médio   | Ajustar limites após observabilidade             |
| Validação de senha quebrar cadastro existente        | Médio   | Aplicar apenas a novos cadastros/trocas de senha |
| Logs exporem dados sensíveis                         | Alto    | Nunca logar senha, token completo ou hash        |
| Configuração ausente em ambiente local               | Baixo   | Atualizar `.env.example` e docs                  |

---

## Dependências

- Epic 1 atualizado para arquitetura JWT custom
- Sistema atual de login/registro funcional
- Variável `JWT_SECRET` definida nos ambientes

---

## Definition of Done

- [ ] Implementação concluída
- [ ] Testes relevantes executados
- [ ] `npm run lint` executado
- [ ] `npm run typecheck` executado
- [ ] `npm test` executado ou justificativa registrada
- [ ] Checklist desta story atualizado
- [ ] Arquivos alterados documentados nesta story

---

## Dev Agent Record

### Arquivos previstos

- `apps/api/src/routes/auth.ts`
- `apps/api/src/middleware/auth.ts`
- `apps/api/src/config/jwt.ts`
- `apps/api/src/utils/validate-password.ts`
- `apps/api/src/index.ts`
- `apps/web/composables/useAuth.ts`
- `apps/web/components/login/RegisterForm.vue`
- `apps/web/components/login/LoginForm.vue`
- `.env.example`

### Notas

- Priorizar correções P0 antes de qualquer melhoria cosmética.
- Não reintroduzir Supabase Auth.
- Manter compatibilidade com o modelo multi-tenant atual.

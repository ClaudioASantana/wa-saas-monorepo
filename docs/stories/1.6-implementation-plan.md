# Plano de Implementação: Story 1.6 - Security Hardening

**Story:** [1.6](./1.6.story.md)  
**Data:** 2026-07-07  
**Estimativa Total:** 8 pontos (≈ 1-2 dias)

---

## 🎯 Objetivo

Implementar as 4 correções críticas de segurança P0 no sistema de autenticação JWT custom, seguindo uma ordem lógica que minimiza riscos e permite rollback em caso de problemas.

---

## 📋 Sequência de Implementação

### Fase 1: JWT_SECRET Obrigatório (30 min) - P0

**Por quê primeiro?** Correção mais simples e crítica. Falha rápida previne deploy inseguro.

#### Passo 1.1: Centralizar configuração JWT (10 min)

```bash
# Arquivo: apps/api/src/config/jwt.ts
```

**Ações:**

1. Abrir `apps/api/src/config/jwt.ts`
2. Adicionar no início do arquivo:

   ```typescript
   const JWT_SECRET = process.env.JWT_SECRET

   if (!JWT_SECRET) {
     throw new Error('FATAL: JWT_SECRET environment variable is required')
   }

   export { JWT_SECRET }
   ```

**Teste:** Tentar iniciar API sem `JWT_SECRET` - deve falhar imediatamente.

---

#### Passo 1.2: Remover fallbacks inseguros (15 min)

```bash
# Arquivos afetados:
# - apps/api/src/routes/auth.ts (2 ocorrências)
# - apps/api/src/middleware/auth.ts (1 ocorrência)
```

**Ações:**

1. **Em `apps/api/src/routes/auth.ts`:**
   - Adicionar import: `import { JWT_SECRET } from '../config/jwt'`
   - Linha ~72: Trocar `process.env.JWT_SECRET || 'secret'` por `JWT_SECRET`
   - Linha ~144: Trocar `process.env.JWT_SECRET || 'secret'` por `JWT_SECRET`

2. **Em `apps/api/src/middleware/auth.ts`:**
   - Adicionar import: `import { JWT_SECRET } from '../config/jwt'`
   - Linha ~25: Trocar `process.env.JWT_SECRET || 'secret'` por `JWT_SECRET`

**Teste:** Login e registro devem continuar funcionando normalmente.

---

#### Passo 1.3: Atualizar .env.example (5 min)

```bash
# Arquivo: .env.example
```

**Ações:**

1. Abrir `.env.example`
2. Adicionar ou atualizar:
   ```env
   # JWT Secret (OBRIGATÓRIO - gere com: openssl rand -base64 32)
   JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars
   ```

**Teste:** Documentação está clara para novos desenvolvedores.

---

### Fase 2: Rate Limiting (45 min) - P0

**Por quê segundo?** Proteção contra ataques de força bruta. Não afeta funcionalidade normal.

#### Passo 2.1: Instalar dependência (5 min)

```bash
cd apps/api
npm install @fastify/rate-limit
```

**Teste:** Verificar que instalou corretamente com `npm list @fastify/rate-limit`

---

#### Passo 2.2: Configurar plugin global (15 min)

```bash
# Arquivo: apps/api/src/index.ts
```

**Ações:**

1. Adicionar import no topo:

   ```typescript
   import rateLimit from '@fastify/rate-limit'
   ```

2. Registrar plugin ANTES das rotas:
   ```typescript
   // Rate limiting global
   await app.register(rateLimit, {
     global: true,
     max: 100, // 100 requests
     timeWindow: '15 minutes',
     errorResponseBuilder: (request, context) => ({
       statusCode: 429,
       error: 'Too Many Requests',
       message: `Limite de requisições excedido. Tente novamente em ${Math.ceil(context.ttl / 1000)} segundos.`,
     }),
   })
   ```

**Teste:** Fazer 101 requests rápidas - deve retornar 429 na 101ª.

---

#### Passo 2.3: Rate limit específico para /auth/login (15 min)

```bash
# Arquivo: apps/api/src/routes/auth.ts
```

**Ações:**

1. Modificar a rota `POST /auth/login`:
   ```typescript
   app.post<{ Body: LoginBody }>(
     '/auth/login',
     {
       config: {
         rateLimit: {
           max: 5,
           timeWindow: '15 minutes',
         },
       },
     },
     async (request, reply) => {
       // código existente...
     }
   )
   ```

**Teste:**

- Fazer 5 logins com senha errada - deve funcionar
- No 6º deve retornar 429
- Aguardar 15 min ou reiniciar servidor - deve funcionar novamente

---

#### Passo 2.4: Rate limit específico para /auth/register (10 min)

```bash
# Arquivo: apps/api/src/routes/auth.ts
```

**Ações:**

1. Modificar a rota `POST /auth/register`:
   ```typescript
   app.post<{ Body: RegisterBody }>(
     '/auth/register',
     {
       config: {
         rateLimit: {
           max: 3,
           timeWindow: '1 hour',
         },
       },
     },
     async (request, reply) => {
       // código existente...
     }
   )
   ```

**Teste:** Tentar criar 4 contas seguidas - a 4ª deve retornar 429.

---

### Fase 3: Validação Robusta de Senha (45 min) - P0

**Por quê terceiro?** Afeta apenas novos cadastros. Não impacta usuários existentes.

#### Passo 3.1: Criar helper de validação (20 min)

```bash
# Arquivo: apps/api/src/utils/validate-password.ts (NOVO)
```

**Ações:**

1. Criar arquivo `apps/api/src/utils/validate-password.ts`
2. Implementar:

   ```typescript
   export interface PasswordValidationResult {
     valid: boolean
     error?: string
   }

   export function validatePassword(password: string): PasswordValidationResult {
     if (password.length < 8) {
       return {
         valid: false,
         error: 'Senha deve ter pelo menos 8 caracteres',
       }
     }

     if (!/[A-Z]/.test(password)) {
       return {
         valid: false,
         error: 'Senha deve conter pelo menos uma letra maiúscula',
       }
     }

     if (!/[a-z]/.test(password)) {
       return {
         valid: false,
         error: 'Senha deve conter pelo menos uma letra minúscula',
       }
     }

     if (!/[0-9]/.test(password)) {
       return {
         valid: false,
         error: 'Senha deve conter pelo menos um número',
       }
     }

     if (!/[^A-Za-z0-9]/.test(password)) {
       return {
         valid: false,
         error: 'Senha deve conter pelo menos um caractere especial (!@#$%^&*)',
       }
     }

     return { valid: true }
   }
   ```

**Teste:** Criar testes unitários ou testar manualmente no Node REPL.

---

#### Passo 3.2: Aplicar validação em /auth/register (15 min)

```bash
# Arquivo: apps/api/src/routes/auth.ts
```

**Ações:**

1. Adicionar import:

   ```typescript
   import { validatePassword } from '../utils/validate-password'
   ```

2. Substituir validação existente (linha ~100-102):

   ```typescript
   // ANTES:
   if (password.length < 6) {
     return reply.status(400).send({ error: 'Senha deve ter pelo menos 6 caracteres' })
   }

   // DEPOIS:
   const passwordValidation = validatePassword(password)
   if (!passwordValidation.valid) {
     return reply.status(400).send({ error: passwordValidation.error })
   }
   ```

**Teste:**

- Tentar cadastrar com "123456" - deve rejeitar
- Tentar cadastrar com "Abc123!@" - deve aceitar

---

#### Passo 3.3: Feedback visual no frontend (10 min)

```bash
# Arquivo: apps/web/components/login/RegisterForm.vue
```

**Ações:**

1. Adicionar validação client-side no campo senha
2. Mostrar requisitos abaixo do campo:
   ```vue
   <div class="text-xs text-gray-500 mt-1">
     <p>A senha deve conter:</p>
     <ul class="list-disc list-inside">
       <li>Mínimo 8 caracteres</li>
       <li>Letra maiúscula</li>
       <li>Letra minúscula</li>
       <li>Número</li>
       <li>Caractere especial (!@#$%^&*)</li>
     </ul>
   </div>
   ```

**Teste:** UI mostra requisitos claramente para o usuário.

---

### Fase 4: Frontend - Tratamento de Erros (30 min) - P1

**Por quê por último?** Melhoria de UX. Não é bloqueante para segurança.

#### Passo 4.1: Tratar erro 429 em useAuth() (15 min)

```bash
# Arquivo: apps/web/composables/useAuth.ts
```

**Ações:**

1. Atualizar função `login`:

   ```typescript
   } catch (error: any) {
     console.error('Login error:', error)

     if (error?.status === 429 || error?.statusCode === 429) {
       return { error: 'Muitas tentativas de login. Aguarde alguns minutos e tente novamente.' }
     }

     const message = error?.data?.message || error?.message || 'Erro ao fazer login'
     return { error: message }
   }
   ```

2. Aplicar mesma lógica em `register`

**Teste:** Exceder rate limit - deve mostrar mensagem amigável.

---

#### Passo 4.2: Substituir window.location.href (10 min)

```bash
# Arquivo: apps/web/components/login/LoginForm.vue
```

**Ações:**

1. Linha ~76, substituir:

   ```typescript
   // ANTES:
   window.location.href = '/'

   // DEPOIS:
   await navigateTo('/', { replace: true })
   ```

**Teste:** Login deve redirecionar suavemente sem reload completo.

---

#### Passo 4.3: Adicionar loading states (5 min)

```bash
# Arquivos: LoginForm.vue e RegisterForm.vue
```

**Ações:**

1. Garantir que botões mostram loading durante requisição
2. Desabilitar inputs durante loading

**Teste:** UX suave durante autenticação.

---

## ✅ Checklist de Validação Final

Após completar todas as fases:

### Backend

- [ ] API não inicia sem `JWT_SECRET`
- [ ] Não existe mais `|| 'secret'` no código
- [ ] Rate limit global funciona (429 após 100 req/15min)
- [ ] Rate limit em `/auth/login` funciona (429 após 5 req/15min)
- [ ] Rate limit em `/auth/register` funciona (429 após 3 req/1h)
- [ ] Senha "123456" é rejeitada
- [ ] Senha "Abc123!@" é aceita
- [ ] Login com credenciais válidas funciona
- [ ] Registro cria tenant + agent em transação

### Frontend

- [ ] Erro 429 mostra mensagem amigável
- [ ] Requisitos de senha são exibidos no cadastro
- [ ] Login redireciona com `navigateTo` (sem reload)
- [ ] Loading states funcionam corretamente

### Documentação

- [ ] `.env.example` atualizado com `JWT_SECRET`
- [ ] Checklist da Story 1.6 atualizado
- [ ] Commit message clara (ex: "feat: implement security hardening (Story 1.6)")

---

## 🔄 Ordem de Commits Sugerida

```bash
# Commit 1: JWT_SECRET obrigatório
git add apps/api/src/config/jwt.ts
git add apps/api/src/routes/auth.ts
git add apps/api/src/middleware/auth.ts
git add .env.example
git commit -m "fix(auth): make JWT_SECRET required, remove insecure fallback"

# Commit 2: Rate limiting
git add apps/api/package.json apps/api/package-lock.json
git add apps/api/src/index.ts
git add apps/api/src/routes/auth.ts
git commit -m "feat(auth): add rate limiting to prevent brute force attacks"

# Commit 3: Password validation
git add apps/api/src/utils/validate-password.ts
git add apps/api/src/routes/auth.ts
git add apps/web/components/login/RegisterForm.vue
git commit -m "feat(auth): implement robust password validation"

# Commit 4: Frontend improvements
git add apps/web/composables/useAuth.ts
git add apps/web/components/login/LoginForm.vue
git commit -m "feat(auth): improve error handling and UX"
```

---

## ⏱️ Estimativa de Tempo

| Fase                       | Tempo Estimado | Prioridade |
| -------------------------- | -------------- | ---------- |
| Fase 1: JWT_SECRET         | 30 min         | P0         |
| Fase 2: Rate Limiting      | 45 min         | P0         |
| Fase 3: Validação de Senha | 45 min         | P0         |
| Fase 4: Frontend UX        | 30 min         | P1         |
| **Total**                  | **2h30min**    | -          |

**Nota:** Adicione 30-60 min para testes manuais e ajustes.

---

## 🚨 Rollback Plan

Se algo der errado em produção:

### Fase 1 (JWT_SECRET)

- **Sintoma:** API não inicia
- **Rollback:** Definir `JWT_SECRET` no ambiente
- **Prevenção:** Validar variável antes de deploy

### Fase 2 (Rate Limiting)

- **Sintoma:** Usuários legítimos bloqueados
- **Rollback:** Aumentar limites via config
- **Prevenção:** Monitorar métricas de 429

### Fase 3 (Password Validation)

- **Sintoma:** Usuários não conseguem cadastrar
- **Rollback:** Temporariamente reduzir requisitos
- **Prevenção:** Testar com senhas reais comuns

### Fase 4 (Frontend)

- **Sintoma:** Problemas de navegação
- **Rollback:** Reverter para `window.location.href`

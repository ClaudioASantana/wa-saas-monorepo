# Story 2.5.1: Respostas Rápidas (Quick Replies)

**Status: ✅ Done**

Como um agente, quero poder usar atalhos rápidos para enviar mensagens frequentes, para que eu possa atender mais clientes em menos tempo.

## Acceptance Criteria

1.  **Gerenciamento (CRUD)**: Administradores e Supervisores podem criar, editar e excluir respostas rápidas no painel de configurações.
2.  **Atalho no Chat**: Ao digitar `/` no campo de mensagem do `ChatWindow.vue`, uma lista pop-over deve aparecer com as respostas disponíveis.
3.  **Variáveis Dinâmicas**: O sistema deve substituir variáveis como `{contato}` pelo nome do contato atual.
4.  **Seleção e Envio**: O agente pode selecionar uma resposta via teclado (setas + enter) ou mouse, preenchendo o campo de mensagem.
5.  **Multitenancy**: Respostas rápidas são isoladas por `workspace_id`.

## Technical Details

- **Database**: Tabela `quick_replies` em `supabase/migrations/20260311000002_create_quick_replies.sql`.
- **Backend**: API CRUD em `apps/web/server/api/workspace/[id]/quick-replies/`.
- **Frontend**: Componente de pop-over integrado diretamente no `ChatWindow.vue` com filtro reativo.

## Checklist

- [x] Criar tabela `quick_replies` via migração Supabase
- [x] Implementar API CRUD (`index.get.ts`, `index.post.ts`, `[replyId].patch.ts`, `[replyId].delete.ts`)
- [x] Criar componente de gerenciamento (`QuickRepliesManager.vue`) nas configurações do workspace
- [x] Implementar atalho `/` no `ChatWindow.vue` com pop-over de seleção e filtro reativo
- [x] Implementar navegação por teclado (setas + Enter + Escape)
- [x] Adicionar suporte a variáveis dinâmicas (`{contato}` → nome do contato)
- [x] Integrar `QuickRepliesManager` na página de configurações
